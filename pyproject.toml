[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rainbow-energy-client"
version = "0.1.0"
description = "Profile-driven client for reading energy device measurements over Modbus (serial or TCP)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
dependencies = [
  "jsonschema>=4.26.0",
  "pymodbus[serial]",
  "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
  "pre-commit>=4.6.0",
  "pytest>=8.3",
  "pytest-cov>=7.1.0",
  "ruff>=0.9",
]

[tool.hatch.build.targets.wheel]
packages = ["src/rainbow_energy_client"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
  "--cov=rainbow_energy_client",
  "--cov-report=term-missing",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "D"]

[tool.ruff.lint.pydocstyle]
convention = "google"
